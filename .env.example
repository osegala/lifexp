# Copy to .env in the backend project root. Never commit .env.
# Generate private values with: python3 scripts/setup-local-env.py
JWT_SECRET=
DB_URL=jdbc:postgresql://localhost:5432/productivity
DB_USERNAME=
DB_PASSWORD=
DEV_SEED_TEST_ACCOUNT=false
DEV_TEST_ACCOUNT_PASSWORD=
